.venv/
__pycache__/
*.py[cod]
.DS_Store
.env
data/raw/
data/processed/
artifacts/live/
artifacts/*.log

# data/interim/ (the chronological train/dev/test_pool split + retrieval
# corpus) is intentionally NOT ignored. make reproduce and the retriever
# both depend on it, and it isn't regenerated by any make target that
# doesn't itself require the raw Kaggle download + API keys. Excluding it
# made a fresh clone unable to reproduce anything.

hiver_support_agent.egg-info/
